<div class="flex items-center justify-between mb-6">
  <h1 class="text-2xl font-bold text-gray-900">编辑小节 - <%= @lesson.title %></h1>
  <%= link_to '返回小节列表', admin_course_chapter_lessons_path(@course, @chapter), class: 'text-sm text-gray-600 hover:text-gray-800' %>
</div>

<div class="hl-card p-6">
  <%= render 'form', course: @course, chapter: @chapter, lesson: @lesson %>
</div>